# Copy to .env.local (or set in Vercel project settings) and change the values.

# Required in production. Admin password for /admin.
# In development only, falls back to "generationbread2026" if unset.
ADMIN_PASSWORD=

# Optional. Secret used to sign admin session cookies.
# Derived from ADMIN_PASSWORD when unset.
ADMIN_SESSION_SECRET=

# Canonical site URL used for SEO (metadata, sitemap, robots, canonical).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Required. Postgres connection string (database is the source of truth
# for products, orders, media, and admin edits). Use Neon in production.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require

# Optional. Directory for legacy file-backed data (used only by
# scripts/migrate-to-neon.mjs to import existing data). Defaults to ./.data
# DATA_DIR=
